<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="loginpref_switch"
        android:summary="Merkt sich die Login Daten um dauerhaft eingeloggt zu bleiben"
        android:title="Automatisch einloggen" />

    <ListPreference
        android:key="loginpref_serveraddress"
        android:defaultValue="1"
        android:entries="@array/pref_server_titles"
        android:entryValues="@array/pref_server_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Gadgeothek Server auswählen" />

</PreferenceScreen>
